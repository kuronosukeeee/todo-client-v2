import { AppBar, Box, Toolbar } from '@mui/material';

const Header = () => {
	return (
		<Box>
			<AppBar position="static">
				<Toolbar></Toolbar>
			</AppBar>
		</Box>
	);
};

export default Header;
